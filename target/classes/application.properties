# EarnX-3 — base application properties
# Profile-specific overrides: application-dev.yml, application-prod.yml
spring.application.name=EarnX-3



spring.flyway.repair-on-migrate=true
# Active profile — override with -Dspring.profiles.active=prod in production
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
